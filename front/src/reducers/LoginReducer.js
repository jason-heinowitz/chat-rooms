// TODO: complete authentication block before starting
import * as types from '../constants/LoginReducerTypes';

const initialState = {
  loggedIn: false,
};

const LoginReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default LoginReducer;
